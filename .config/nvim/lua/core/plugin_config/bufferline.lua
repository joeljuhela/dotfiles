require('bufferline').setup({
    options = {
        offsets = {
            filetype = 'neo-tree',
            text = 'Neo-tree',
            highlight = 'Directory',
            text_align = 'left'
        }
    }
})
